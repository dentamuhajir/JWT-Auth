spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_jwt
    username: root
    password: zcbsdbsdgwegwet23
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl.update: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.MySQL8Dialect

